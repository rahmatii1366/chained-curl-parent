import { Routes } from '@angular/router';
import { ChainedCurlComponent } from './view/chained-curl/chained-curl.component';

export const routes: Routes = [
//     { path: '', redirectTo: 'chained-curl/ui', pathMatch: 'full' },
    { path: "", component: ChainedCurlComponent }
];
